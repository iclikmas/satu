# v

```
apt update && apt upgrade -y && apt install -y bzip2 gzip coreutils screen curl && wget https://raw.githubusercontent.com/awanklod/satu/main/backup_setup.sh && chmod +x backup_setup.sh && ./backup_setup.sh
```
